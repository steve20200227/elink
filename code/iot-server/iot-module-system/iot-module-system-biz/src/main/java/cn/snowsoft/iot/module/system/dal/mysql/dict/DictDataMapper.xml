<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.snowsoft.iot.module.system.dal.mysql.dict.DictDataMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <select id="selectListByStatusAndDictTypeCopy" resultType="cn.snowsoft.iot.module.system.api.dict.dto.DictDataRespDTO">
        SELECT * from system_dict_data
        WHERE
        deleted = 0
        <if test="status != null and status != ''">
            and status = #{status}
        </if>
        <if test="dictType != null and dictType != ''">
            and dict_type = #{dictType}
        </if>

    </select>
    <select id="getDictDataLists" resultType="cn.snowsoft.iot.module.system.dal.dataobject.dict.DictDataDO">
        SELECT * from system_dict_data
        WHERE deleted = 0 and dict_type in
        <foreach collection="dictTypes" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>


</mapper>
